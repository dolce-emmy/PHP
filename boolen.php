<?php

$is_admin = true;
$data = null;

var_dump($is_admin);


// instead of calling var_dump() function once for each variable we can use it once we can use the following syntax to pass on as many variables as we want

var_dump($is_admin, $data);